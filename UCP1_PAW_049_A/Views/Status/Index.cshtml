@model IEnumerable<UCP1_PAW_049_A.Models.Status>

@{
    ViewData["Title"] = "Index";
}

<h1>Data Status</h1>

<p>
    <a asp-action="Create">Tambah Data Baru</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Status")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Status1)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdStatus"><i class="fa fa-marker fa-md"></i></a> |
                    <a asp-action="Details" asp-route-id="@item.IdStatus"><i class="fa fa-list"></i></a> |
                    <a asp-action="Delete" asp-route-id="@item.IdStatus"><i class="fa fa-trash-alt fa-md"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
